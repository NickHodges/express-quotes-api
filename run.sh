

#!/bin/bash

# Print a message to indicate the script is starting
echo "Starting the Express app..."

# Run the TypeScript Express app using ts-node
npx ts-node src/index.ts

# Print a message to indicate the script has finished
echo "Express app has stopped."
